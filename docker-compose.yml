services:
  dashboard:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount the pipeline's forecast output so new runs appear without a
      # rebuild. Defaults to the gitignored ./forecast_output at the repo root;
      # the image seeds a copy from web/public so the container also works
      # standalone 